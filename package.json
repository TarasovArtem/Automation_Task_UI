{
  "author": "Artem Tarasov",
  "description": "Automation Task - UI",
  "scripts": {
    "cypress": "cypress open",
    "cy:run:chrome": "cypress run --spec 'cypress/integration/**/*' --headless --browser chrome",
    "cy:run:firefox": "cypress run --spec 'cypress/integration/**/*' --headless --browser firefox",
    "cy:run:edge": "cypress run --spec 'cypress/integration/**/*' --headless --browser edge"
  },
  "devDependencies": {
    "cypress": "^8.7.0"
  }
}
