/// <reference types="cypress" />

import Navigation from '../page-object/navigation';

describe('', () => {
    const navigation = new Navigation();

    beforeEach(() => {
        navigation.navigate();
    })

    it('', () => {

    })
})